{
  "name": "flostfound-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute flostfound-db --local --file=src/db/schema.sql"
  },
  "dependencies": {
    "hono": "^4.4.0"
  }
}
